#include <iostream>
#include <string>
#include <vector>
#define REP(i, n) for (int i = 0; (i) < (int)(n); ++ (i))
#define REP3(i, m, n) for (int i = (m); (i) < (int)(n); ++ (i))
#define REP_R(i, n) for (int i = (int)(n) - 1; (i) >= 0; -- (i))
#define REP3R(i, m, n) for (int i = (int)(n) - 1; (i) >= (int)(m); -- (i))
#define ALL(x) ::std::begin(x), ::std::end(x)
using namespace std;


std::vector<auto> solve(int64_t N, int K, int Q, const std::vector<int64_t> &A, const std::vector<int64_t> &L) {
    // TODO: edit here
}

// generated by oj-template v4.8.1 (https://github.com/online-judge-tools/template-generator)
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int64_t N;
    int K, Q;
    std::cin >> N >> K;
    std::vector<int64_t> A(K);
    std::cin >> Q;
    std::vector<int64_t> L(Q);
    REP (i, K) {
        std::cin >> A[i];
    }
    REP (i, Q) {
        std::cin >> L[i];
    }
    auto ans = solve(N, K, Q, A, L);
    REP (i, (int)ans.size()) {
        std::cout << a[i] << ' ';
    }
    std::cout << '\n';
    return 0;
}
